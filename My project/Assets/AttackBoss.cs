using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;
#if UNITY_EDITOR
using UnityEditor;
#endif
public class AttackBoss : MonoBehaviour
{
    //Collider myCollider;
    //public int countBlob = 2;
    //class SpawnQueueElement
    //{
    //    public GameObject obj;
    //    public Target target;
    //    public Rigidbody rb;
    //    public float remainingTime;
    //    public PathSystem.PathData pathData = new PathSystem.PathData();
    //}
    //// Start is called before the first frame update
    //void Start()
    //{

    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    if (myCollider.tag == ("Boss"))
    //    {
    //        if (myCollider.GetComponent<Target>().GetCurrencHealce() <= 70)
    //        {
    //            Vector3 randomPosition = GetRandomPositionInRoom();
    //            for (int i = 0; i < countBlob; i++)
    //            {
    //                Instantiate(SpawnQueueElement.obj, randomPosition);
    //            }
    //        }
    //        if (myCollider.GetComponent<Target>().GetCurrencHealce() <= 50)
    //        {
    //            Vector3 randomPosition1 = GetRandomPositionInRoom();
    //            for (int i = 0; i < countBlob+1; i++)
    //            {
    //                Instantiate(SpawnQueueElement.obj, randomPosition1);
    //            }
    //        }
    //        if (myCollider.GetComponent<Target>().GetCurrencHealce() <= 30)
    //        {
    //            Vector3 randomPosition2 = GetRandomPositionInRoom();
    //            for (int i = 0; i < countBlob+2; i++)
    //            {
    //                Instantiate(SpawnQueueElement.obj, randomPosition2);
    //            }
    //        }
    //        if (myCollider.GetComponent<Target>().GetCurrencHealce() <= 10)
    //        {
    //            Vector3 randomPosition3 = GetRandomPositionInRoom();
    //            for (int i = 0; i < countBlob+3; i++)
    //            {
    //                Instantiate(SpawnQueueElement.obj, randomPosition3);
    //            }
    //        }
    //    }
    //}
    //public float minX = -0.3097f; // Минимальное значение по оси X
    //public float maxX = -1.47f; // Максимальное значение по оси X
    //public float minY = -0.3913f; // Минимальное значение по оси Y
    //public float maxY = 0.417f; // Максимальное значение по оси Y
    //public float minZ = -1.47f; // Минимальное значение по оси Z
    //public float maxZ = 0.433f; // Максимальное значение по оси Z
    //Vector3 GenerateRandomVector()
    //{
    //    float x = Random.Range(minX, maxX);
    //    float y = Random.Range(minY, maxY);
    //    float z = Random.Range(minZ, maxZ);

    //    return new Vector3(x, y, z);
    //}
}

